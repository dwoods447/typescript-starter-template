{
  "name": "typescript-starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-ui": "cd ./ui && npm install && npm run build",
    "build-server": "cd ./server && npm install && npm run serve",
    "build-project": "npm run build-ui && npm run build-server"
  },
  "author": "Demaria Woods",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.2",
    "dotenv": "^16.0.3",
    "tslint": "^6.1.3",
    "typescript": "^4.9.4"
  },
  "devDependencies": {
    "@types/node": "^18.11.18"
  }
}
